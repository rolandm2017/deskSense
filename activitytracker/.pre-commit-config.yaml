repos:
    - repo: local
      hooks:
          - id: sync-types
            name: Sync TypeScript types
            entry: bash -c 'cd activitytracker && source .venv-linux2/bin/activate && python scripts/sync_types.py'
            language: system
            files: '.*\.py$' # Only run when Python files change
            pass_filenames: false
